.catalog:
  name: "Eco Server"
  version: "0.7.2.5-beta"
  description: "Atlassian Crowd identity service provider"
  uuid: eco
  questions:
      - variable: DATA_PATH
        label: Local path to data directory for Eco Server or ref name
        description: Local path to data directory for Eco Server or ref name
        required: true
        type: string
        default: "/data/eco"
      - variable: CONFIG_PATH
        label: Local path to config directory for Eco Server or ref name
        description: Local path to config directory for Eco Server or ref name
        required: true
        type: string
        default: "/data/eco-config"
